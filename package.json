{
  "name": "selenia-aroma",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "dev:3100": "next dev -p 3100",
    "build": "next build",
    "start": "next start",
    "start:3100": "next start -p 3100",
    "lint": "eslint",
    "test": "node --experimental-strip-types --test --disable-warning=ExperimentalWarning \"src/**/*.test.ts\"",
    "db:migrate": "node scripts/d1-migrate.mjs --remote",
    "db:migrate:local": "node scripts/d1-migrate.mjs",
    "db:tables": "wrangler d1 execute selenia-aroma --remote --yes --config cloudflare/wrangler.toml --command \"select name from sqlite_master where type='table' order by name\"",
    "db:tables:local": "wrangler d1 execute selenia-aroma --local --config cloudflare/wrangler.toml --command \"select name from sqlite_master where type='table' order by name\"",
    "db:counts": "wrangler d1 execute selenia-aroma --remote --yes --config cloudflare/wrangler.toml --command \"select 'stores' t, count(*) n from stores union all select 'profiles', count(*) from profiles union all select 'base_blends', count(*) from base_blends union all select 'essential_oils', count(*) from essential_oils union all select 'mood_categories', count(*) from mood_categories\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.86.0",
    "lucide-react": "^0.562.0",
    "next": "16.2.6",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.6",
    "tailwindcss": "^4",
    "typescript": "^5",
    "wrangler": "^4.129.0"
  }
}
